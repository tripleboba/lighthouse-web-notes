const locations = ["Gibraltar Point", "Peggy's Point", "Cove Island", "Discovery Island", "Cape Scott", "Point Clark", "Kincardine"];
const nameLength = locations.map(name => name.length);

console.log(nameLength);